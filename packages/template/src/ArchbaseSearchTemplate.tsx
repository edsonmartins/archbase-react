/**
 * ArchbaseSearchTemplate — template de busca com filtros, resultados e binding ao DataSource (placeholder).
 * @status experimental
 */
import React from 'react'

export function ArchbaseSearchTemplate() {
  return <div>SEARCH TEMPLATE</div>
}
