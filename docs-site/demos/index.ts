// Demos dos componentes de entrada (editors)
export * as archbaseEditDemos from './components/editors';

// Exportar demos individuais para importação direta
export {
  usage as archbaseEditUsage,
  withDataSource as archbaseEditWithDataSource,
  withValidation as archbaseEditWithValidation,
  withSearch as archbaseEditWithSearch,
  sizes as archbaseEditSizes,
  states as archbaseEditStates,
  stylesApi as archbaseEditStylesApi,
} from './components/editors';
