import catalog from '../../component-catalog.json';
import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Recipes/Notifications Recipes" />

# Notifications Recipes

Aponte para `ArchbaseNotifications` e `ArchbaseAlert` via catálogo quando documentar toasts e mensagens de status.

<Story name="Links">
  {() => (
    <ul>
      {['ArchbaseNotifications', 'ArchbaseAlert']
        .map((name) => catalog.components.find((c) => c.name === name))
        .filter(Boolean)
        .map((component) => (
          <li key={component?.name}>
            <strong>{component?.name}</strong>: <a href={component?.canonicalUrl}>{component?.canonicalUrl}</a>
          </li>
        ))}
    </ul>
  )}
</Story>

Use esse recipe para mostrar exemplos de toast programático e fallback de alerta.
