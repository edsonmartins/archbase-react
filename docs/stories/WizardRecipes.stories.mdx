import catalog from '../../component-catalog.json';
import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Recipes/Wizard Recipes" />

# Wizard Recipes

Documente o uso de `ArchbaseFormTemplate` + `ArchbaseAdvancedTabs`/`ArchbaseSpaceTemplate` com `canonicalUrl` para o wizard/stepper.

<Story name="Links">
  {() => (
    <ul>
      {['ArchbaseFormTemplate', 'ArchbaseSpaceTemplate']
        .map((name) => catalog.components.find((c) => c.name === name))
        .filter(Boolean)
        .map((component) => (
          <li key={component?.name}>
            <strong>{component?.name}</strong>: <a href={component?.canonicalUrl}>{component?.canonicalUrl}</a>
          </li>
        ))}
    </ul>
  )}
</Story>

Enfatize `steps`, `onSubmit`, `validation` e instruções de a11y (aria-current) nas receitas baseadas em wizard.
