import catalog from '../../component-catalog.json';
import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Recipes/Remote Select" />

# Remote Select Recipes

Use `ArchbaseAsyncSelect`, `ArchbaseLookupSelect`, `ArchbaseTreeSelect` (quando aplicável) com `canonicalUrl` do catálogo para mostrar busca remota.

<Story name="Links">
  {() => (
    <ul>
      {['ArchbaseAsyncSelect', 'ArchbaseLookupSelect', 'ArchbaseTreeSelect']
        .map((name) => catalog.components.find((c) => c.name === name))
        .filter(Boolean)
        .map((component) => (
          <li key={component?.name}>
            <strong>{component?.name}</strong>: <a href={component?.canonicalUrl}>{component?.canonicalUrl}</a>
          </li>
        ))}
    </ul>
  )}
</Story>

Documente debounce/search e binding com dataSource usando o dataset do Storybook [link]. Também destaque `onChangeValue`.
