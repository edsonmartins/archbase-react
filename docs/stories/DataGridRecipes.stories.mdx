import catalog from '../../component-catalog.json';
import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Recipes/DataGrid Recipes" />

# DataGrid Recipes

Referencie `ArchbaseDataGrid`, `ArchbaseDataGridToolbar` e `ArchbaseDetailPanel` usando os `canonicalUrl` para demonstrar loading/empty/actions.

<Story name="Links">
  {() => (
    <ul>
      {['ArchbaseDataGrid', 'ArchbaseDataGridToolbar', 'ArchbaseDetailPanel']
        .map((name) => catalog.components.find((c) => c.name === name))
        .filter(Boolean)
        .map((component) => (
          <li key={component?.name}>
            <strong>{component?.name}</strong>: <a href={component?.canonicalUrl}>{component?.canonicalUrl}</a>
          </li>
        ))}
    </ul>
  )}
</Story>

Use essas referências em MDX/receitas para documentar ações, exportação e painéis detalhados que o Storybook já mostra.
